import { createContext } from 'react';

// Create the supplier context
const SupplierContext = createContext();

// Export both the context and its type
export { SupplierContext };
export default SupplierContext;